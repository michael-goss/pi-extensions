# One guideline per non-empty line. Lines beginning with # are ignored.
Use pi_scout before broad/open-ended codebase exploration, especially when you expect to inspect more than three files or perform repository-wide search.
When calling pi_scout, provide a concrete problem description, desired goal, and any known scope; wait for the scout result before continuing.
