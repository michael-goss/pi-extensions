## Pi Scout context hygiene
When local codebase exploration is broad, open-ended, or likely to require more than three file reads, call the `pi_scout` tool first with a focused problem description and goal. Wait for its compact artifact before continuing in the parent session.
